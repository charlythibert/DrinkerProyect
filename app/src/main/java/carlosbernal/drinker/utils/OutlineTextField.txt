        focusedTextColor = Color.White, // Color del texto cuando está enfocado
        unfocusedTextColor = Color.Gray, // Color del texto cuando no está enfocado
        disabledTextColor = Color.LightGray, // Color del texto cuando está deshabilitado
        errorTextColor = Color.Red, // Color del texto cuando está en estado de error
        focusedContainerColor = Color.Black, // Color del contenedor cuando está enfocado
        unfocusedContainerColor = Color.DarkGray, // Color del contenedor cuando no está enfocado
        disabledContainerColor = Color.Gray, // Color del contenedor cuando está deshabilitado
        errorContainerColor = Color.Red, // Color del contenedor en estado de error
        cursorColor = Color.Red, // Color del cursor
        errorCursorColor = Color.Red, // Color del cursor cuando hay error
        focusedIndicatorColor = Color.Green, // Color del indicador cuando está enfocado
        unfocusedIndicatorColor = Color.Gray, // Color del indicador cuando no está enfocado
        disabledIndicatorColor = Color.LightGray, // Color del indicador cuando está deshabilitado
        errorIndicatorColor = Color.Red, // Color del indicador cuando hay error
        focusedLeadingIconColor = Color.White, // Color del icono izquierdo cuando está enfocado
        unfocusedLeadingIconColor = Color.Gray, // Color del icono izquierdo cuando no está enfocado
        disabledLeadingIconColor = Color.Gray, // Color del icono izquierdo cuando está deshabilitado
        errorLeadingIconColor = Color.Red, // Color del icono izquierdo cuando hay error
        focusedTrailingIconColor = Color.White, // Color del icono derecho cuando está enfocado
        unfocusedTrailingIconColor = Color.Gray, // Color del icono derecho cuando no está enfocado
        disabledTrailingIconColor = Color.Gray, // Color del icono derecho cuando está deshabilitado
        errorTrailingIconColor = Color.Red, // Color del icono derecho cuando hay error
        focusedLabelColor = Color.Magenta, // Color de la etiqueta cuando está enfocado
        unfocusedLabelColor = Color.Gray, // Color de la etiqueta cuando no está enfocado
        disabledLabelColor = Color.LightGray, // Color de la etiqueta cuando está deshabilitado
        errorLabelColor = Color.Red, // Color de la etiqueta cuando hay error
        focusedPlaceholderColor = Color.LightGray, // Color del placeholder cuando está enfocado
        unfocusedPlaceholderColor = Color.DarkGray, // Color del placeholder cuando no está enfocado
        disabledPlaceholderColor = Color.Gray, // Color del placeholder cuando está deshabilitado
        errorPlaceholderColor = Color.Red, // Color del placeholder cuando hay error
        focusedSupportingTextColor = Color.White, // Color del texto de soporte cuando está enfocado
        unfocusedSupportingTextColor = Color.Gray, // Color del texto de soporte cuando no está enfocado
        disabledSupportingTextColor = Color.Gray, // Color del texto de soporte cuando está deshabilitado
        errorSupportingTextColor = Color.Red, // Color del texto de soporte cuando hay error
        focusedPrefixColor = Color.White, // Color del prefijo cuando está enfocado
        unfocusedPrefixColor = Color.Gray, // Color del prefijo cuando no está enfocado
        disabledPrefixColor = Color.Gray, // Color del prefijo cuando está deshabilitado
        errorPrefixColor = Color.Red, // Color del prefijo cuando hay error
        focusedSuffixColor = Color.White, // Color del sufijo cuando está enfocado
        unfocusedSuffixColor = Color.Gray, // Color del sufijo cuando no está enfocado
        disabledSuffixColor = Color.Gray, // Color del sufijo cuando está deshabilitado
        errorSuffixColor = Color.Red // Color del sufijo cuando hay error